<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-3.2.xsd">

    <!-- enable use-expressions -->
    <http auto-config="true" use-expressions="true">
        <!--<intercept-url pattern="/cartelera/**" access="permitAll" />-->
        <!--<intercept-url pattern="/administrador/**" access="hasRole('ROLE_ADMIN')" />-->
        <!--<intercept-url pattern="/admin**" access="hasRole('ROLE_ADMIN')" />-->
        <!--<intercept-url pattern="/login/**" access="permitAll" />-->
        <!--<intercept-url pattern="/**" access="permitAll" />-->



        <!-- access denied page -->
        <access-denied-handler error-page="/407" />

<!--        <form-login
                login-page="/login/cargarLogin.action"
                default-target-url="/welcome"
                authentication-failure-url="/login/login.action?error"
                username-parameter="username"
                password-parameter="password" />
        <logout logout-success-url="/login?logout"  />-->
        <logout logout-url="/logout" logout-success-url="/todos/cartelera/cargarCartelera.action" />
        <!-- enable csrf protection -->
       <!-- <csrf/>-->
    </http>


  <!--  <authentication-manager alias="AuthenticationManager">
        <authentication-provider>
            <user-service>
                <user name="cesar" password="123456" authorities="ROLE_USER" />
            </user-service>
        </authentication-provider>
    </authentication-manager>-->

    <!-- Select users and user_roles from database -->
<authentication-manager alias="AuthenticationManager">
        <authentication-provider>
            <jdbc-user-service data-source-ref="dataSource"
                               users-by-username-query=
                                       "select cedula,password,enable from user where cedula=?"
                               authorities-by-username-query=
                                       "Select c.cedula, r.name from user c, roles r where c.role_id = r.id and c.cedula =?" />
        </authentication-provider>
    </authentication-manager>

</beans:beans>
